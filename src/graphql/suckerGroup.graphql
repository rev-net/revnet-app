query SuckerGroup($id: String!) {
  suckerGroup(id: $id) {
    id
    tokenSupply
    projects {
      items {
        chainId
        projectId
        token
        currency
        decimals
        balance
      }
    }
  }
}
