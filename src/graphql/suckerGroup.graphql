query SuckerGroup($id: String!) {
  suckerGroup(id: $id) {
    id
    tokenSupply
    projects {
      items {
        balance
        chainId
        currency
        decimals
        projectId
        token
        tokenSupply
        tokenSymbol
        version
        suckerGroupId
      }
    }
  }
}
