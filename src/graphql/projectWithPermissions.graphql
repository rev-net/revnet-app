query ProjectWithPermissions($projectId: Float!, $chainId: Float!, $version: Float!) {
  project(projectId: $projectId, chainId: $chainId, version: $version) {
    projectId
    chainId
    version
    owner
    permissionHolders {
      items {
        account
        operator
        permissions
      }
    }
  }
}
